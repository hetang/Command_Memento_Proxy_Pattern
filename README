I have used all above pattern to create movie database. In this functionality, I used memento pattern for storing state of the Inventory. Command pattern used to make operation changable at runtime. Proxy Pattern used to provide virtual view to client where all methods are encapsulated inside Proxy and Proxy is making actual call to diffrent reciever.